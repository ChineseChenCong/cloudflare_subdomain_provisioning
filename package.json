{
  "name": "cloudflare-subdomain-provisioning",
  "version": "2.0.0",
  "description": "Cloudflare Workers 二级域名分发服务（支持多账户、加密、邮箱验证）",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 execute cf --local --file=./migrations/0001_init.sql && wrangler d1 execute cf --local --file=./migrations/0003_multi_account_encryption.sql && wrangler d1 execute cf --local --file=./migrations/0004_email_verify_announcements.sql",
    "db:migrate:remote": "wrangler d1 execute cf --remote --file=./migrations/0001_init.sql && wrangler d1 execute cf --remote --file=./migrations/0003_multi_account_encryption.sql && wrangler d1 execute cf --remote --file=./migrations/0004_email_verify_announcements.sql"
  },
  "dependencies": {
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "typescript": "^5.6.0",
    "wrangler": "^3.90.0"
  }
}
